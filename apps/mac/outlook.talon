app: com.microsoft.Outlook
-
archive:
	key(ctrl-e)

flag:
	key(ctrl-5)

unflag:
	key(cmd-alt-')

junk:
	key(cmd-shift-j)

move:
	key(cmd-shift-m)

move to [<user.text>]:
	key(cmd-shift-m)
	insert(user.text or "")

reply:
	key(cmd-r)

reply all:
	key(cmd-shift-r)